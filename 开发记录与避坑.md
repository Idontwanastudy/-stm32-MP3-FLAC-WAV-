# 开发记录与避坑（活档案）

> 本文件是项目的**活档案**：记录"开发过什么 + 踩过哪些雷 + 怎么解决的"。
> 每次排错/功能完成后，把"现象→根因→解决方案"按模板追加到对应项目小节。
> 详细过程可配合 auto-memory 与历史会话使用。

## 记录模板

- 日期：
- 项目 / 模块 / 涉及文件：
- 现象：（用户看到什么）
- 根因：（为什么，越具体越好）
- 解决方案：（怎么改的，关键代码要点）
- 复用提示：（这个坑会不会在其他项目/芯片上再遇到）

---

## 一、STM32F407ZGT6 音乐播放器

**位置**：`D:\Users\15754\Desktop\Music_player_F407ZGT6`（STM32CubeIDE，从 F103 移植）

**硬件**：STM32F407ZGT6(168MHz) + PCM5102a DAC + OLED(I2C1, 400kHz) + SD 卡(SDIO 4-bit) + W25Q64(SPI1) + FreeRTOS + I2S2 DMA 双缓冲 + USB MSC

**软件框架**：FreeRTOS + FatFs；`Music_Driver.c` 按后缀分发 WAV / FLAC / MP3（`audio_file_read()`）；I2S2 DMA 乒乓双缓冲 + half/full 回调；软件线性插值重采样（源样本左对齐 32bit）；链接脚本 RAM 128KB + CCM 64KB(0x10000000)

### 已实现
- WAV 16bit / 24bit 播放正常
- FLAC / MP3 播放已接通（仍有问题，见下）
- OLED 显示（含滚动字幕、汉字）、按键 EXTI(PF0~PF4)、SD 卡挂载、W25Q64、USB MSC

### ★代码清理（2026-09-22）: 诊断代码已全部移除
排错用的"临时脚手架"不该长期留在产品路径上，本次全部撤掉（结论只留在本档案 + 技能速查表里）：
- **本次为"按音量键概率停歌"加的原因码体系**：`stop_reason` / `stop_key_count` / `stop_glitch_count` / `show_stop_diag()`（OLED 第 4 行 `R.. K.. G.. S..`），及其在 5 个停机点的赋值、3 个等待循环里的调用、`Music_Driver.h` 的 extern —— **全部移除**。
- **SD 排错期的老诊断**：`sd_init_stage` / `sd_init_error`（`bsp_driver_sd.c/.h` 的定义与全部赋值）、`g_sd_err_count` / `g_sd_last_err`（`sd_diskio.c`）、OLED 上的"阶段/码/错"显示（`sd_wait_card` 里的 `char dbg[]` 块、`"SD失败 阶段:%d 码:%d 错误:%d"`）—— 全部移除；挂载失败现在只显示 **"SD卡挂载失败, 请检查"**。
- **MP3 排错期的三处 `char dbg[17]` 打印块**（`d-2 r%lu` / `d%d s%lu` / `t%lu l%lu e%d`）＋ 随之无用的局部变量 `seek_rc` —— 移除。
- **调试用显示**：`OLED_ShowNum(2, 1, audio_type, 1)`（把音频类型号打在屏上）、WAV 损坏屏的 `错误码:%d,%d` 行 —— 移除。
- **注释掉的死代码**：`//OLED_ShowString(...歌名)`、`//audio_stop();`、`//OLED_Clear();`、`//OLED_ShowNum(1,1,SPI2->I2SPR,4)`、`//f_close(&SDFile);`、`//printf("Scanning audio files...")`、`//static uint8_t WAV_RAW[8192];`、`/*...Not audio files...*/` —— 移除。
- **★保留（这些是功能代码，不是诊断）**：`g_sd_io_err`（拔卡检测）、PF5 停止键的**双沿 + 最小时长过滤**（`key_stop_down`/`key_stop_tick`/`STOP_KEY_MIN_MS`）、`WM8960_BeforeClockStop()`/`AfterClockStart()`；`main.c` 开机的 `WM8960 OK/ERR` 是状态指示，保留。
- 清理后 5 个文件括号/圆括号全部平衡（脚本核对）；`Music_Driver.c` 58652 → 55166 字节。
- **复用提示**：**诊断代码是"临时脚手架"，问题定性后要主动撤掉** —— 否则它会永久留在产品路径上（多占 I2C/CPU、拖慢关键流程，还让后来的人分不清哪些是业务逻辑、哪些是排查用的）。撤之前先把"现象→根因→方案"写进档案，知识就不会丢。

### 版本发布（v1.0，2026-09-22）
- 仓库: GitHub **Idontwanastudy/-stm32-MP3-FLAC-WAV-**（public，默认分支 main）；`README.md`（详细版，重点写 v1.0 改了什么）已写好。
- **★重要: 该仓库 main 上原本是「F103 版」工程**（`Music_Player_FreeRTOS.ioc` / `STM32F103ZETX_FLASH.ld` / 用户 8-12 写的 README）→ **发布前必须先把 F103 存成一个分支**（网页: 分支选择器 → 新建 `F103` from main），再用 `git push -u origin main --force` 把 F407 v1.0 覆盖到 main（两边历史无关, 不加 --force 会被拒）。这样 F103 与 F407 两版都在仓库里。
- 远端 remote 已配成 SSH: `git@github.com:Idontwanastudy/-stm32-MP3-FLAC-WAV-.git`（用户在仓库里加了 Deploy key；**私钥在其本机，沙箱无法推送**——实测 `Permission denied (publickey)`)。
- **✅已推送(2026-09-22)**: 远端 `main` = F407 v1.0（提交 `47d1acc`，252 文件，Debug 未上传）；标签 `v1.0` 已推；**F103 版安全保留在分支 `V1.0-F103Ver`**（旧的 `main` 提交 `3b6be54`）。
- **硬件资料已并入仓库（2026-09-22 追加）**: 桌面 `PCB LAYOUT & Gerber & BOM` 整份复制到工程目录 **`Hardware/`**（21 文件 / 26MB：立创EDA 工程 `*.eprj2`、GERBER 三个 zip、BOM 三个 xlsx、原理图三个 PDF、以及 `_backup/` 里 10 个自动保存快照 + `*.eprj_backup`）并推送到 `main`。发布前已扫描确认**无个人信息**（手机号/邮箱/联系人/地址）。以后 PCB 再改，建议直接改项目里的 `Hardware/` 这份（或改完桌面的那份后同步过来）再 `git add -A && git commit && git push`。
- 推送方式: 沙箱内生成了**一次性 ed25519 部署密钥**（私钥只留在沙箱、未落进工程目录），用户把公钥加为该仓库的 Deploy key（**必须勾选 Allow write access**）→ 沙箱用 `git push --force-with-lease origin main` + `git push origin v1.0` 完成。用完可删掉该 Deploy key。
- `README.md` 已**按用户原 F103 README 的风格**（徽章 + 表格 + mermaid 架构图 + 开发日志）重写为 F407 v1.0 版，含"v1.0 更新内容"专节；首行注明"F407 版=main、F103 版=分支 V1.0-F103Ver"。
- 远端 `main` 上原本是 **F103 版**工程（`Music_Player_FreeRTOS.ioc` / `STM32F103ZETX_FLASH.ld` / 用户 8-12 写的 README 8642B）→ 发布前先在网页建分支 **`F103`** 保存旧版（分支选择器 → 输入 F103 → Create branch from main），再 `git push -u origin main --force`（两边历史无关，不加 --force 会被拒）。README 里已写明"F407 版=main、F103 版=分支 F103"。
- 本地 git: `main` 分支 + 提交 + tag **`v1.0`**（`ef092ec`，252 文件，`.git` 3MB）。
- 顺带修正: `.ioc` 里遗留的 `ProjectManager.ProjectName=\ Music_Player_F407ZGT6`（转义的前导空格）→ 去掉，避免 clone 后 CubeMX 认不出工程。
- 已知小瑕疵（已写进 README）: `stm32f4xx_hal_conf.h` 的 `HSE_VALUE` 仍是 CubeMX 默认 25M，而本板是 8MHz（`system_stm32f4xx.c` 里已单独定义 8M）；实际计时由 FreeRTOS 的 SysTick(按 168MHz 配置) 提供，不影响功能，只影响 HAL 自己上报的时钟值。
- 发布方式: 用户习惯网页操作 → 用 **GitHub Desktop** 接管本地仓库一键 Publish；tag `v1.0` 已在本地，网页发 Release 时选它即可（GitHub Desktop 不一定自动推 tag）。
- **注意**: 本沙箱默认**不允许删除文件**，git 因此无法清理 `index.lock`/临时对象 → 提交会失败；需要用 `allow_cowork_file_delete` 授权后才能正常 git 操作。

### 雷点清单（按模块）

#### 内存 / 链接脚本
- **RAM 溢出链接失败** → 大缓冲（work_buf/FLAC 解码器/MP3 缓冲）放 `.ccmram`(0x10000000)。
- **CCM 不能给 DMA 用** → SD 读取必须走 CPU 轮询（`f_read`），SDIO 已改轮询，不要贸然配 SDIO DMA（且 DMA2_Stream3 已被 SPI1_TX 占用）。

#### 时钟 / 启动 / 烧录
- **启动即 HardFault**：链接脚本 RAM 长度写错（F407 是 0x20000000 连续 128KB SRAM + 0x10000000 独立 64KB CCM，**不是 192K 连续**）→ `_estack = 0x20030000` 超出物理 SRAM，复位一压栈就进 `HardFault_Handler` → RAM 改回 128K。
- **8MHz 无源晶振时钟**：main.c `HSE_BYPASS → HSE_ON`、`PLLM 25→8`；`system_stm32f4xx.c` `HSE_VALUE 25M→8M`；`.ioc` 同步（HSE-Crystal / HSE_VALUE=8M / PLLM=8）防 CubeMX 覆盖。漏改任何一处主频都会错。
- **FreeRTOS port 必须匹配 FPU**：hardfp 编译（fpv4-sp-d16）必须用 **ARM_CM4F** port（保存 FPU 上下文）；ARM_CM3 port 不保存 FPU 寄存器 → 硬浮点下上下文损坏。
- **CubeIDE 下载报错** `Cannot invoke ICProject.getProject() ... is null`：调试配置损坏（launch 找不到关联工程）→ Run→Debug Configurations→删掉 F407 配置→右键 **Debug As → STM32 Cortex-M C/C++ Application** 自动重建；或删工作区 `.metadata\...\.launches\` 里的 `.launch` 文件。
- **ST-Link "Error finishing flash operation"**：烧录时板子失联（硬件/连接问题，非固件）→ 排查顺序：① 板子独立 USB 供电（ST-Link 3.3V 带不动整板，最常见）；② SWD 三线 SWDIO/SWCLK/GND 接对；③ SWD 频率 4MHz→1MHz；④ 换 USB 线/口。
- **Flash 占用**：.elf 体积(2MB)含调试信息，实际看 `.text+.rodata`；本工程约 368KB/1MB（代码 109KB + 字库 259KB）。

#### OLED（I2C1）
- **黑屏**：I2C1 TX DMA 误配 `DMA_CIRCULAR` → 循环模式 DMA 完成后状态永远 BUSY，`HAL_I2C_Master_Transmit_DMA` 全失败 → 改 `DMA_NORMAL`，且同步改 `.ioc` 防 CubeMX 覆盖。
- **滚动字幕拖慢播放**：滚动刷新每次 I2C 发 256 字节（阻塞几毫秒），放在播放任务循环里会让 DMA 音频缓冲欠载 → 把滚动/显示移到**独立低优先级任务**，播放任务只渲染到缓冲、不碰 I2C。
- **汉字上下颠倒**（如"音"变成立+日倒过来）→ 字模纵向取模**位序**问题 → 重新生成字库，核对取模方向。

#### SD 卡（SDIO）
- **挂载失败 res=3 / 阶段255**：卡识别失败 → 查硬件（卡/接触/上拉/供电）。
- **4-bit 切换失败关键坑**：
  1. `hsd.Init.BusWide` 初始化必须与卡一致（1B），否则 `ConfigWideBusOperation` 读 SCR 报 `ILLEGAL_CMD`(0x2000)。
  2. `HAL_SD_ConfigWideBusOperation` 内部**不清 ErrorCode** → 失败后必须先 `hsd.ErrorCode = HAL_SD_ERROR_NONE` 再回退。
  3. 稳定方案：1-bit 初始化 → 清错误码 → 切 4-bit → 失败回退 1-bit；`ClockDiv=4`(8MHz)，约 4MB/s。
- **读扇区错误码**：码 2/8 = D0(PC8) 数据线 CRC/超时（硬件问题）；码 4 = CMD 响应超时。
- **注意**：CubeMX 重新生成会覆盖 `sdio.c`/`bsp_driver_sd.c` 的手改 → 重要改动同步改 `.ioc` 并留备份。

#### SD 卡热插拔（拔卡检测）
- **拔卡后不立即提示（2026-08-24，已修复）**：
  - **根因**：`FATFS/Target/sd_diskio.c` 里 `#define SD_TIMEOUT 30*1000`。HAL 的 `HAL_SD_ReadBlocks` 用 `(HAL_GetTick()-tickstart) >= Timeout` 判断，**Timeout 单位是毫秒** → 实际是 **30 秒超时**。拔卡后读操作要空转 30 秒才返回错误，所以提示迟迟不来。
  - **修复**：`SD_TIMEOUT` 改为 **300ms**（正常读一次 <1ms，300ms 余量充足）。
  - **同时**：`audio_file_read` 挂载失败时区分 `FR_NOT_READY`(=无卡) → 显示 **"SD卡已拔出 / 请插入SD卡"** 并**每 300ms 重试挂载等待插回**；插回后重新扫描。其它错误仍显示"SD失败 阶段/码/错误"。
  - **注意**：本工程**没有卡检测(CD)引脚**，`BSP_SD_IsDetected()` 永远返回 `SD_PRESENT`（空实现）。所以拔卡检测**只能靠读/挂载失败**，缩短超时是关键。
  - **复用提示**：ST HAL 的 SD 超时参数是**毫秒**（用 HAL_GetTick 计时），ST 默认 `sd_diskio.c` 给的 30 秒对"热插拔检测"太长。要快速检测拔卡就把 SD_TIMEOUT 调小（100~500ms）。没有 CD 引脚时只能靠 I/O 失败检测。

#### SD 卡热插拔：拔卡检测完善（2026-08-24 第二轮）
- **现象**：拔卡时**偶尔**不显示提示；插回后不重新挂载；挂载失败不重试。
- **根因（两个）**：
  1. **检测点太少**：原来只在"切歌时 `audio_file_read` 的 `f_mount`"一处检测。**在"已停止/按按键3"等待状态下完全不检测**（停在那儿拔卡毫无反应）。
  2. **插回后抢跑**：插回成功即 `return`，此时 `osSemaphoreRelease(LOAD_OR_NOT)` 触发的重新扫描还没跑完，可能继续用**陈旧文件列表**。
- **修复**：新增两个函数（`Music_Driver.c`）：
  - `sd_card_present()` — 快速判断卡在不在（`f_mount` 成功即"在"）。**只能在没打开文件时调用**（会重挂卷）。
  - `sd_wait_card()` — 显示"SD卡已拔出/请插入SD卡"，**每 300ms 重试挂载**直到插回（或 USB 占用/按停止）；插回后显示"SD卡已插入/重新扫描..."并触发重扫。
  - `audio_file_read` 挂载失败走 `sd_wait_card()`；**三个等待循环（"已停止/按按键3"）每 ~500ms 调 `sd_card_present()`**，拔卡立即进提示。
- **注意**：播放中**不能**调 `sd_card_present()`（会重新挂载、破坏已打开的 SDFile）→ 播放中拔卡靠**读失败**（SD_TIMEOUT 已改 300ms）→ 歌曲结束 → 回到 `audio_file_read` → 挂载失败 → 提示。

#### SD 卡热插拔：磁盘层统一检测 + 提示完善（2026-08-24 第三轮）
- **现象**：FLAC 播放中拔卡不报错（MP3/WAV 会立刻报错）；插回卡后没有"重新扫描"提示。
- **根因**：
  1. **检测只靠读出错误**，但 FLAC 的解码器在 `f_read` 失败后会用 0 填充继续解，路径与 WAV/MP3 不同 → 表现不一致。
  2. 插回后提示一闪而过（调用方立刻重画"已停止/按按键3"）。
- **修复**：
  - **在磁盘 I/O 层统一记录**：`FATFS/Target/sd_diskio.c` 新增 `volatile uint8_t g_sd_io_err`，`SD_read`/`SD_write` 失败时置 1、成功时清 0。
  - `Music_Driver.c` 三个播放循环（WAV/MP3/FLAC）都加 `if (g_sd_io_err) { g_sd_io_err=0; audio_stop(); file_ended=1; break; }` —— **与格式无关，一律立即停止**。
  - `sd_wait_card()` 插回成功后 `osDelay(1500)` 停留显示"SD卡已插入/重新扫描..."。
- **复用提示**：**多格式共用一条 I/O 通路时，把"设备异常"统一在 I/O 层打标记**，比在每个上层解析器里各自判断可靠得多（各解析器对读失败的传播方式可能不同）。

#### SD 卡热插拔：★★真正的根因（2026-08-24 第四轮）
- **现象**：插回 SD 卡**永远不重新挂载**（提示停在"SD卡已拔出"不变），FLAC 播放中拔卡也不报错。
- **根因（关键）**：`FATFS/Target/sd_diskio.c` 里的磁盘状态变量 `static volatile DSTATUS Stat`。
  - `SD_read` 开头有 `if (Stat & STA_NOINIT) return RES_NOTRDY;`，但 **`SD_read` 失败时没把 `Stat` 置回 `STA_NOINIT`**。
  - 所以拔卡后 `Stat` 仍表示"已初始化" → 插回后 `f_mount` → FatFs 见"已初始化"**跳过 `disk_initialize`（不重新识别卡）**，直接读引导扇区 → 失败 → 返回 **`FR_DISK_ERR`（不是 `FR_NOT_READY`）** → 拔卡分支不命中，重试永远失败。
- **修复**：`SD_read`/`SD_write` **失败时执行 `Stat |= STA_NOINIT;`** → 下次 `f_mount`/读会重新走 `SD_initialize`→`BSP_SD_Init` 重新识别卡。**插回后即可正常重挂**。
- **复用提示**：ST 生成版 `sd_diskio.c` 的 `Stat` 只在 `SD_initialize` 里更新，**I/O 失败不会让它失效** → 热插拔(拔卡再插)后无法恢复。要支持热插拔，**任何 I/O 失败都要 `Stat |= STA_NOINIT`**。另外错误码要按 `FR_DISK_ERR` 判断，不能只判 `FR_NOT_READY`。

#### SD 卡热插拔：★★★插回后无法重挂 — BusWide 残留（2026-08-24 第五轮）
- **现象**：拔卡→插回后**永远挂载不上**（提示不变）。Debug 看到 `SD_CMD=13(CMD13)`, `response_r1=0x40010400` → **CURRENT_STATE(bit9-12)=2=IDENT**（不是 TRAN=4），即卡**没被初始化到传输态**。
- **根因**：`BSP_SD_Init` 成功切到 4-bit 后会执行 `hsd.Init.BusWide = SDIO_BUS_WIDE_4B;`，**下次初始化前没有复位回 1-bit**。插回卡时 `HAL_SD_Init` 用残留的 **4-bit** 配置初始化刚插入、**默认 1-bit** 的卡 → 读 SCR 错配 → 初始化失败 → 卡停在 IDENT → 永远挂不上。（与"4-bit 切换"那条坑同源）
- **修复**（`FATFS/Target/bsp_driver_sd.c`）：`BSP_SD_Init` 里 **`HAL_SD_Init` 之前**加：
  ```c
  hsd.Init.BusWide = SDIO_BUS_WIDE_1B;   /* 每次初始化都从 1-bit 起步 */
  hsd.State = HAL_SD_STATE_RESET;        /* 强制 HAL_SD_Init 重跑 MspInit, 彻底复位外设 */
  ```
- **同时**：`flac_fill_buffer_region` / `mp3_fill_buffer_region` 循环开头加 `if (g_sd_io_err) {补0; file_ended=1; return;}`，拔卡后**立即收尾**(不再继续解 0 填充数据 → 之前 FLAC 会"反复播放最后一声")。
- **复用提示**：**支持 SD 热插拔时，所有"一次性修改过、之后被 HAL 复用的 init 结构体字段"都必须在使用前复位**（这里 `hsd.Init.BusWide`）。`HAL_SD_Init` 不会帮你还原；靠 `hsd.State = HAL_SD_STATE_RESET` 可强制重跑 MspInit 做外设级复位。

#### ★切歌"呲呲"声: WM8960 DAC 软静音（2026-08-24 第八轮）
- **现象**: WAV 播放正常(说明 WM8960 模拟侧没问题), 但**切歌/停机时有"呲呲"瞬态噪声**。
- **根因**: `i2s_set_sample_rate()` 在换歌时会重配 PLLI2S → **I2S 时钟(MCLK/BCLK/LRC)频率突变**, 而此时 WM8960 的 DAC 和输出驱动还开着 → 它内部**滤波/电荷泵要随新时钟重新锁定** → 输出"呲呲"。PCM5102a 对此不敏感, **WM8960 敏感**。
- **修复(标准做法: 静音→换时钟→稳定→解除静音)**:
  1. `wm8960.c/.h` 新增 `WM8960_DacMute()`(R5 DACMU[3]=1 数字软静音) / `WM8960_DacUnmute()`(DACMU=0)。软静音是渐进斜坡, 不会有"啪"声。
  2. **所有 `HAL_I2S_DMAStop` 之前**(`Music_Driver.c` 3 个播放流程结尾 + `audio_stop()`): `WM8960_DacMute(); osDelay(20);`(等斜坡走完, 否则切断斜坡会有"啪")
  3. **所有 `HAL_I2S_Transmit_DMA` 之后**(WAV/MP3/FLAC 共 3 处): `osDelay(60); WM8960_DacUnmute();`(等新时钟稳定再放声)
- **同时修**: WM8960 与 OLED **共用 I2C1** → `WM8960_WriteReg` 现在也拿 `oled_mutexHandle`(递归互斥量)护总线, 避免与 OLED 刷新并发打乱 I2C 时序(启动阶段调度器没跑则不加锁)。
- **复用提示**: **换采样率/停 DMA 之前必须先静音 codec**(软静音优先)。寄存器控制的 codec(WM8960 等)对时钟突变敏感, "呲呲/啪"声基本都是时钟切换 + 输出未静音造成的。另外**同一 I2C 总线上的多个器件必须共用一把总线锁**。
- **实测结果(2026-09-22)**: 用户反馈**"减轻了"**(方向正确, 但可能仍有余声)。**未改动的路径**: 暂停/继续用的是 `HAL_I2S_DMAPause/Resume`(DMAPause 时 I2S 仍在跑但无数据, 也可能产生直流跳变/咔哒) → 若还有残留瞬态, 下一步查这条路径。

#### ★音量键"有概率停止正在播放的歌"（2026-09-22 第九轮，排查中）
- **现象**: 按 PF7/PF8 调音量时, **有概率(非必现)**正在播放的曲子直接停掉。
- **代码事实(已逐处核对)**:
  - 能让播放**立刻停止**的只有三条路径, 全在播放循环里: ① `if (g_sd_io_err) { g_sd_io_err=0; audio_stop(); file_ended=1; break; }`(WAV/MP3/FLAC 三处) ② `if (usb_sd_occupied) {...}` ③ `play_cmd == CMD_STOP`(PF5)。其余只有"文件放完自动下一首"。
  - `g_sd_io_err` 在 `sd_diskio.c` 里**任何一次**(含 300ms 超时 / CRC 错)读失败就置 1 → **一次偶发读错 = 立刻停歌**。而 SDIO 现在跑 **8MHz(ClockDiv=4)**, 本项目在 8MHz 上**已知会偶发读错**(见 SD 第三/四轮记录, 当时曾特意回退 6MHz)。
  - 音量键做的事 = 软件定时器回调里**一整套 I2C 突发**: WM8960 写 R2/R3(2 次) + `OLED_ShowString(4,…)`(~130 次) + `OLED_RefreshScreenWithScroll()`(256 次) ≈ **380 次 I2C 事务 / ~15ms 忙等**, 外加一个 `sprintf`。CPU/总线负载明显, 但音频任务优先级(24)高于定时器任务(2), 所以**不会直接饿死音频**。
  - `configASSERT` 定义 = `if((x)==0){taskDISABLE_INTERRUPTS(); for(;;);}` → **任何 FreeRTOS 断言失败 = 关中断死循环(整机冻结)**; 且 **`configCHECK_FOR_STACK_OVERFLOW` 未定义** → 栈溢出**完全无检测**。
  - 定时器任务栈 `configTIMER_TASK_STACK_DEPTH = 256`(=1KB)。用 `Debug/**/*.su` 逐层核算回调最深路径: prvTimerTask 24 + prvProcessTimerOrBlockTask 24 + prvProcessExpiredTimer 40 + OLED_SCROLL_Callback 40 + oled_show_volume 24 + OLED_ShowString 24 + OLED_ShowChinese 32 + OLED_GB2312_GetIndex 24 + OLED_ShowChar 24 + OLED_WriteData 16 + osMutexAcquire 32 + xQueueSemaphoreTake 64 ≈ **368 字节**(+ 中断异常帧 104 字节, 因 `configENABLE_FPU=1` 惰性压栈) ≈ **470/1024 字节** → **余量约 550 字节, 不足以判定栈溢出**(sprintf 未计, newlib-nano 整数路径 ~200B 仍在余量内)。→ **"栈溢出"不是主嫌, 不能当结论。**
- **候选根因(按可能性排序)**:
  - **A. SD 偶发读错 → `g_sd_io_err` → 播放循环立刻 `audio_stop()`**。天生概率性、最符合"有概率"; 8MHz 已知偶发错; 音量键的 I2C 突发可能只是把本就临界的余量压垮(或纯巧合)。
  - **B. `usb_sd_occupied` 抖动**: `StartDefaultTask` 每 200ms 轮询 `hUsbDeviceFS.dev_state == USBD_STATE_CONFIGURED` 并同步该标志, 一旦为 1 → `audio_stop()` + 显示"USB已连接/暂停播放"。
  - **C. 卡死**: `OLED_WriteData/WriteCommand` 里 `while(HAL_I2C_GetState(&hi2c1) != HAL_I2C_STATE_READY);` **无超时自旋**; 一旦卡住就**持着 `oled_mutexHandle` 不放** → 音频任务下首歌调 `WM8960_DacMute()` 时 `osMutexAcquire(osWaitForever)` **永久阻塞** → 播放逻辑死。或 configASSERT/ HardFault 关中断死循环。
  - **D. WM8960 收到一次坏 I2C 写** → 写到别的寄存器(如 R5 DACMU=1) → 静音/掉电, 表现为"歌还在走但没声"。
- **★用户提供的关键现象(2026-09-22)**: 停歌那一刻**屏幕显示歌名 + "按按键3 开始播放"**, 按按键3 能重新播放当前曲子。
  - **这条信息直接排除**了"卡死/断言死循环/内存被踩/WM8960 掉电静音"(那些要么屏幕冻住、要么歌还在走没声)。
  - 屏幕出现"按按键3/开始播放"= 代码走到 `audio_file_read` 里 `play_state == STATE_STOPPED` 的等待循环 —— 而 **`play_state = STATE_STOPPED` 只有 `audio_stop()` 会设**。所以停止来源**只可能是**: ① 停止键 PF5 → `CMD_STOP` → `audio_stop()`; ② `g_sd_io_err`(SD 一次读失败) → `audio_stop()`。USB 那条会另外显示"USB已连接"。
  - (第 1 行的"已停止"被滚动缓冲重绘回歌名, 所以看起来是"歌名 + 按按键3" —— 与用户描述完全一致。)
  - 另外记录: `HAL_GPIO_EXTI_Callback` 里 **PF5 停止键完全没有去抖**(其他键都有), 一次按下可能产生多个沿(只是重复置 `CMD_STOP`, 本身无害), 但若接线串扰导致误触发, 表现就是"没碰停止键却停歌"。
- **✅已加诊断(2026-09-22, 已改代码, 待用户编译实测)**:
  - `Music_Driver.c`: 新增 `stop_reason`(1=SD读失败 2=USB占用 3=停止键PF5 0xFF=未记录) / `stop_key_count`(PF5 EXTI 触发次数); 在**所有**能停机的位置打上原因码(WAV/MP3/FLAC 三个播放循环的 `g_sd_io_err`/`usb_sd_occupied`/`CMD_STOP`、两个 fill 函数、MP3 失败分支、PF5 EXTI 计数)。
  - 新增 `show_stop_diag()`: 把结果打到 OLED 第 4 行, **限流每秒一次**; 在三个"已停止"等待循环里调用(第 4 行不参与滚动刷新, 不会被覆盖)。
  - `FATFS/Target/sd_diskio.c`: 新增 `g_sd_err_count`(累计失败次数) / `g_sd_last_err`(最近一次 `hsd.ErrorCode`, 状态检查失败置 0x80), 在 4 个失败分支自增。
  - **读法**: `R1` = SD 读失败停的(看 S/E: E8=DATA 超时/卡停顿, E2=DATA CRC, E99=状态检查失败/无卡); `R2` = USB; `R3` = 停止键(**若没按停止键而 K 在涨 = 误触发**, 查按键接线/串扰); `Ru` = 走了以上三条以外的路径(**说明还有未找到的停机路径, 这本身就是关键线索**)。
- **★诊断结果(2026-09-22, 用户实测): `R3 K1 S0 E0`** —— **是 PF5 停止键的 EXTI 被触发了一次**(`CMD_STOP` → `audio_stop()`), **SD 一次都没错(S0/E0), USB 也不是**。
  - 即: 按音量键时 **PF5 引脚上出现了一个下降沿**。PF5 只配了内部上拉(约 40k)且**完全没去抖**, 原来只认下降沿就立刻 `play_state = STOPPED`。
  - 机理: 音量键一按 = 一串 I2C + CPU 突发 → 本板**电源/地完整性差**(之前已用"给 WAV 加 CPU 负载也会卡"证明过) → PF5 这种弱上拉的长线输入在这种扰动下出 µs~ms 毛刺 → EXTI。所以表现为"**有概率**"。
- **✅已修复(2026-09-22): PF5 改"双沿触发 + 最小时长过滤"**(纯软件防毛刺, 不动其它按键)
  - `gpio.c`: PF5 单独配置为 **`GPIO_MODE_IT_RISING_FALLING`**(PF0~PF4 仍只下降沿);`.ioc` 同步 `PF5.GPIO_ModeDefaultEXTI=GPIO_MODE_IT_RISING_FALLING`。
  - `Music_Driver.c` EXTI 回调: 读回电平为低 = 刚发生下降沿 → 记按下起点(抖动期间不重置);电平为高 = 刚上升沿 → 结算时长,**`>= STOP_KEY_MIN_MS`(20ms)才算真按键**,否则计入 `stop_glitch_count`(丢弃、不动作)。
  - OLED 第 4 行: `R3 K1 G5 S0` —— **K=真按键次数, G=被判为毛刺而丢弃的次数**。按音量键时若 **G 涨而 K 不涨**, 毛刺+过滤同时被证实。
  - **行为变化**: 停止键现在**松手时才动作**(且需按住 ≥20ms)。正常按键仍能停歌, 只是时机从"按下"变"松开"。
- **若修复后按音量键仍会停歌(K 仍在涨 = 毛刺持续时间 >20ms)** → 说明不是瞬时毛刺, 而是**持续性串扰/接错线**(如 PF5 与音量键共用回线、并排走线耦合), 要查硬件;可再把 `STOP_KEY_MIN_MS` 调大或把 PF5 改成和音量键一样的轮询输入。
- **硬件建议**: PF5 引脚就近加 **10k 外部上拉 + 100nF 到地**(RC≈1ms, 硬件级滤掉 µs~ms 毛刺);检查 PF5 走线/回线与音量键的关系;根子还是这块板的电源/地完整性。
- **待办**: ① 上述"按音量键 20 次看 G/K"验证一轮;② 打开 `configCHECK_FOR_STACK_OVERFLOW 2` + 钩子(目前**完全没开**);③ 把 `configASSERT` 的关中断死循环改成"置故障码 + 显示"。
- **建议修复(不管根因是哪个都该做)**: ① **SD 单次错误不要立刻停歌** → 重试/连续 N 次失败才停, 并把 SDIO 时钟回 **6MHz**; ② 音量键的 I2C/显示工作**移出软件定时器回调**(回调只置标志/计数, 由任务执行), 顺带去掉 `sprintf`; ③ I2C 自旋加**超时 + 恢复**(不能无超时死等); ④ 定时器栈 256→512 字, 开栈溢出检查。
- **复用提示**: ① **"任何一次 I/O 错误就停机"在热插拔检测里是捷径, 但会把偶发错误放大成致命停机** → 应做错误计数/重试后再决策; ② **软件定时器回调里只应做轻量、非阻塞的事**(定时器任务栈通常很小; 且回调阻塞会拖住所有软定时器); ③ `configASSERT` 默认实现是关中断死循环 → 现场调试时**先改成可显示/可记录的故障码**; ④ 任务栈用量可以用 CubeIDE 生成的 `Debug/**/*.su` 逐层相加核算, 比猜准得多。

#### ★★切歌"滋滋"残留：MCLK 被停掉 → WM8960 电荷泵塌陷（2026-09-22 第十轮）
- **现象**: 加了 DAC 软静音后切歌"减轻了", 但**仍有一丝"滋滋", 很快就消失**(用户 2026-09-22 反馈)。
- **根因(已核对 HAL 源码)**: `HAL_I2S_DMAStop()` 内部最后会 **`__HAL_I2S_DISABLE(hi2s)`** → **I2S 外设整个关掉 → MCLK/BCLK/LRC 一起停**。而 WM8960 **没用内部 PLL**(R4 CLKSEL=0, SYSCLK 直取 MCLK), 时钟一停它的内部逻辑和**无电容耳机输出的电荷泵/输出级**就塌陷, 下一首重新使能时再建立 → 就是那声"滋滋"。**该瞬态在 DAC 之后, 所以 DACMU 数字静音盖不住**(这解释了"已经静音了却还有声")。PCM5102a 没有电荷泵/寄存器状态, 对时钟中断不敏感 → 换 WM8960 后才出现。
- **另一个白挨的**: `i2s_set_sample_rate()` 原来**无条件** disable/reconfig PLLI2S —— 即使前后两首采样率相同, 每次切歌也要多挨一次时钟中断。
- **修复(两处, 均可一键回退)**:
  1. **停数据流但保持 I2S 外设使能** → MCLK 不停: 新增 `i2s_stream_stop_keep_clock()`, 只 `CLEAR_BIT(CR2, TXDMAEN)` + `HAL_DMA_Abort(hdmatx)` + 清 UDR + `hi2s2.State = READY`, **不关外设**; 5 处 `HAL_I2S_DMAStop(&hi2s2)`(WAV/MP3×2/FLAC 循环结尾 + `audio_stop()`)全部换成它。下一首 `HAL_I2S_Transmit_DMA` 重新起 DMA(源码里它**只在 `I2SE=0` 时才使能外设**, 所以不会打断时钟)。开关宏 `KEEP_MCLK_ON_TRACK_CHANGE`, 置 0 退回原行为。
  2. **采样率没变就不动 PLLI2S**: `i2s_set_sample_rate()` 用 static `cfg_n/cfg_r` 记住当前 N/R, **相同就直接跳过**(PLLI2S/I2S 都不动)。需要真换率时**先 `__HAL_I2S_DISABLE` 再动 PLLI2S**(避免重配期间 MCLK 输出乱时钟), 且 I2SPR 只在这时写。
  3. 另加 `i2s_stream_start()` 包一层: `HAL_I2S_Transmit_DMA` 失败时回退到"关外设→重开→再起 DMA", 避免优化后出现"直接没声音"这种更糟结果。
- **预期**: **同采样率切歌应完全干净**(MCLK 全程连续, WM8960 根本不缺时钟); **跨采样率(44.1k↔48k)仍可能有一丝** —— 那种情况必须真换 MCLK, 时钟一停电荷泵必然塌陷, 软件盖不住。
- **待实测**; 若跨采样率仍有残留, 下一步可在换时钟期间**连耳机输出级一起停**(R26 PWR2 的 LOUT1[6]/ROUT1[5])再恢复, 或从 PCB 给 WM8960 的 MCLK/CPVOUT 做好去耦。
- **★★踩雷回退(2026-09-22 同日实测): 上面第 1 条(保持外设使能)会造成"偶尔切歌整首播放纯噪声, 听不到音乐" → 已把 `KEEP_MCLK_ON_TRACK_CHANGE` 改回 0(默认关)。**
  - **根因**: STM32 I2S 是**按 16 位半槽**从 DR 取数据的(一个 32bit 声道槽要取两次)。原流程 `HAL_I2S_DMAStop`(**关外设, 帧计数器复位**) → 紧接着 `force_i2s_config`(**重新使能 = 新帧开始**) → 几微秒内 `HAL_I2S_Transmit_DMA`(置 TXDMAEN), 三步挨得极近 → **第一个半字必定落在新帧的第一个半槽, 相位天然对齐**。
  - 而"保持外设使能"= 把 DMA 挂到**已经跑了一会儿**的 I2S 上, 起 DMA 时落在哪个半槽**随机**; 若落在后半槽, **每个 32bit 样本的高/低 16 位对调** → 24bit 数据被拆半错位 → **整首纯噪声**(相位一旦错就锁死整首, 所以现象是"某几次切歌后整首没音乐")。
  - **∴ `HAL_I2S_DMAStop` 里那个"关外设再开"的动作不是多余的, 它承担了数据相位对齐的职责。** 想同时做到"MCLK 不停 + 相位正确", 必须改成 **DMA 全程不停转(环形) + 切歌间隙由生产者写静音** 的架构(不重启 DMA 就没有相位问题), 属于较大重构。
  - **保留的安全部分**: 第 2 条(采样率没变就不动 PLLI2S)保留 —— 它对相位无影响, 因为此时 `HAL_I2S_DMAStop` 仍然关了外设, `force_i2s_config` 仍是"紧接重使能+立刻起 DMA"。
- **复用提示(补充)**: ① **不要为了省一次时钟中断而把"DMA 重新挂到正在运行的外设上"** —— 对按半槽取数的 I2S/SPI 类外设, 相位随机 = 数据整体错位(整首纯噪声/纯乱码); ② 排查"整首纯噪声/整首乱码"时, 优先怀疑**数据流相位/字长对齐**, 而不是音质或干扰; ③ 每次改动留一个"一键回退"的编译开关(`KEEP_MCLK_ON_TRACK_CHANGE`), 这次就是靠它 5 分钟回退定位。

#### ★★★WM8960 手册给出的权威依据（2026-09-22，用户上传 WM8960.PDF 后核对）
> 背景：用户**换回原 DAC(PCM5102a) 后"滋滋"消失** → 确认问题在 **WM8960 这颗 codec 本身**(与 STM32 I2S / 播放逻辑无关)，PCM5102a 是纯硬件 DAC、没有依赖 MCLK 的内部状态，所以对它无感。

- **① 决定性证据：手册 "STOPPING THE MASTER CLOCK"(p69) 原文**：
  - "the master clock may be stopped **in Standby and OFF modes**"；"**MCLK should not be stopped while the class D outputs are enabled**, as this would prevent the outputs from functioning."
  - "**NOTE: Before DIGENB can be set, the control bits ADCL, ADCR, DACL and DACR must be set to zero and a waiting time of 1ms must be observed. Any failure to follow this procedure may prevent DACs and ADCs from re-starting correctly.**"
  - **我们的做法错在哪**：切歌时 `HAL_I2S_DMAStop()` 会让 MCLK 直接消失，而**只做了 DACMU 数字软静音(R5 bit3)，DACL/DACR(R26 位8:7)一直开着，也从未等 1ms** → 正好违反手册这条"停时钟前必须关 DAC 并等 1ms"的时序要求。∴ 切歌"滋滋"不是偶发, 是手册明确警告的时序问题。
- **② 片内 PLL 救不了**：手册 clocking 图注明 **"All internal clocks are derived from SYSCLK"**，而 SYSCLK 只能来自 **MCLK 或 PLL 输出**，**PLL 的参考还是 MCLK** → **只要 MCLK 停，WM8960 全部内部时钟都会停**（用内部 PLL 也不行）。所以从器件特性上讲：**WM8960 不允许在播放中随便停 MCLK**。
- **③ 我们完全没用到的 Anti-Pop 寄存器**：
  - **R28 (1Ch) Anti-Pop 1**：bit0 `HPSTBY` **Headphone Amplifier Standby**(1=送待机, 专门用来安静地关耳机放大器)、bit2 `SOFT_ST`(VMID 软启动)、bit7 `POBCTRL`、bit4 `BUFDCOPEN`、bit3 `BUFIOEN`。
  - **R29 (1Dh) Anti-Pop 2**：bit6 `DISOP`(放电耳机隔直电容)、bit5:4 `DRES[1:0]`(放电电阻 400/200/600/150Ω)。
- **④ VROI（R27 bit6）**："Whenever an analogue output is disabled, it remains connected to VREF through a resistor. **This helps to prevent pop noise when the output is re-enabled.**" 默认 500Ω(保持贴近 VREF) → **"切歌期间把 LOUT1/ROUT1 关掉、之后再开"是有手册依据的安全做法**。
- **⑤ DAC 软静音细节(p35)**：DACMU 是渐变斜坡, 速率由 **DACMR(R6 bit2)** 选: 0=快(fs/2, @48k 最大 **10.7ms**)、1=慢(fs/32, 最大 171ms); **DACSMM(R6 bit3)** 决定解除静音是"立即跳回音量(0)"还是"渐升(1)"。手册建议：**播放中软静音用 DACSMM=1；在一首曲子开头解除静音用 DACSMM=0**(否则开头会被压低)。之前那套 `osDelay(20)/(60)` 是拍脑袋定的, 恰好覆盖了默认 DACMR=0 的 10.7ms，但没用上这两个位。
- **两条修复路线（等换回 WM8960 时择一）**：
  - **路 A（彻底、推荐）**：**让 MCLK 在整个会话中永不停止** = DMA 全程不停转(环形) + 切歌间隙由生产者往缓冲写数字静音。codec 全程不缺时钟 → pop/滋滋根源消失, 也不需要在切歌时做上下电。代价: 重构音频主循环(中等)。**前提是 DMA 只在开机起一次、绝不重挂**(否则又是相位错位)。
  - **路 B（最小改动、严格按手册时序）**：切歌时 DACMU 软静音 → 等 ≥10.7ms(或把 DACMR 设为慢速) → **关 DACL/DACR(R26 位8:7)** → **等 ≥1ms** → 这时才 `HAL_I2S_DMAStop`(MCLK 停) → 下一首时钟稳定后**反序**恢复(先起时钟 → 再开 DACL/DACR → 等 → 复位 HPSTBY → DACMU 解除静音)；全程用 **R28 的 `HPSTBY`** 把耳机放大器送 standby 兜住，效果最好。
  - 跨采样率(44.1k↔48k)切歌无论哪条路都有真实时钟跳变，但按手册时序可把瞬态压到最小。
- **✅已实现（2026-09-22，用户选择"按手册来"）: 路 B** —— `wm8960.c` 新增 `WM8960_BeforeClockStop()` / `WM8960_AfterClockStart()`：
  - `BeforeClockStop()`: `DacMute()` → `osDelay(15)`（等软静音斜坡，默认 DACMR=0 最大 10.7ms）→ 写 **R28=0x001（HPSTBY=1，耳机放大器 standby）** → 写 **R26=0x060（DACL/DACR=0，保留 LOUT1/ROUT1）** → `osDelay(3)`（**≥1ms，手册要求**）。**之后调用方才 `HAL_I2S_DMAStop()`**（MCLK 这时才停）。
  - `AfterClockStart()`: 写 R26=0x1E0（重开 DACL/DACR）→ `osDelay(5)` → 写 R28=0x000（退出 standby）→ `osDelay(1)` → `DacUnmute()`（用默认 DACSMM=0 立即回到音量设定，新曲开头不会被渐升压低，手册 p35 建议）。
  - 接线位置: `Music_Driver.c` 里 **5 处 `HAL_I2S_DMAStop` 之前** + **3 处 `HAL_I2S_Transmit_DMA` 之后**（WAV/MP3×2/FLAC 三个流程 + `audio_stop()`），已用脚本核对计数=5/3、括号平衡。
  - **★关键约束**: `AfterClockStart()` 必须在 `HAL_I2S_Transmit_DMA()` **之后**调用，**绝不能插在 `force_i2s_config()`（使能外设）和起 DMA 之间** —— 那段紧凑顺序是 I2S 数据相位对齐的保障（见第十轮那个"整首纯噪声"的坑）。
  - **本方案完全不改 I2S/DMA 的启停顺序和时刻**，只增加 codec 寄存器写 + 延时，所以**没有相位风险**（这也是它"最稳妥"的原因）。
  - **★硬件实况（2026-09-22 用户澄清）: WM8960 是焊在板上的, 板子另有"额外 I2S 排针"供外接 DAC 用。**之前说的"换回原本的 DAC"只是把 PCM5102a 模块插在那排针上听, **WM8960 一直都在 I2C 总线上、`WM8960_Init()` 一直生效**。所以这套寄存器写**是真在跑的**, 会实际改变 WM8960 状态(不是 NACK 掉的空操作)。
  - **★由此得到一个天然的 A/B 参考**: 两路 DAC **共享同一组 I2S 时钟/数据**, 同一个时刻接的是完全一样的数据流。所以切歌时**同时对比两个输出**就能一刀切开责任: PCM5102a 干净而 WM8960 有瞬态 → 瞬态产在 WM8960 芯片内部(与 MCU/数据无关); 两个都有 → 问题在 I2S 数据/时钟侧。
  - **✅✅实测通过（2026-09-22 用户反馈:"完美解决了")** —— 按手册时序(软静音 → R28 HPSTBY standby → 关 DACL/DACR → 等 ≥1ms → 才停 MCLK; 起播反序恢复)后, **切歌"滋滋"彻底消失**。∴ 结论: **"停 MCLK 前必须先把 DAC/ADC 下电并等 ≥1ms"(手册 p69)就是根因**, 只做 DACMU 数字软静音盖不住(那个瞬态产生在 DAC 之后/时钟域)。
  - **若以后换板/换曲目再出现同类瞬态**, 依次可试: ① 把 **DACMR(R6 bit2) 设为 1**(慢斜坡 fs/32, 最大 171ms, 让静音更"软"); ② 切歌期间**连 LOUT1/ROUT1 一起关**(R26=0x000, 手册说关掉的输出经 500Ω 接 VREF 正是为防重新使能时 pop); ③ 上路 A(DMA 全程不停转, 让 MCLK 永不中断)。
- **复用提示**: ① **换 codec 要先问三个问题**——它是否依赖 MCLK 连续？它有没有专门的 Anti-Pop 寄存器？手册对"停时钟/改时钟"的时序要求是什么？(WM8960 三条都有明确条文，我们当年一条都没查) ② **"软静音 ≠ 可以随便断时钟"**：WM8960 是"停时钟前必须先把 DAC/ADC 下电并等 1ms"，软静音只是盖住数据通路; ③ 排查"某个 DAC 有瞬态、另一个没有"时，去比两边的**时钟依赖**和**是否有内部状态**，比调软件参数快得多。
- **复用提示**: ① **"已经软静音了却还有噪声" → 瞬态产生在 DAC 之后**(输出级/电荷泵/时钟域), 软件静音盖不住, 要从"别让 codec 缺时钟"下手; ② **换 codec 前先确认它是否依赖 MCLK**(有内部 PLL 的可容忍 MCLK 中断, 直取 MCLK 的不能); ③ **`HAL_I2S_DMAStop` 会顺手关外设(停时钟)**, 只想停数据流要用"清 TXDMAEN + 中止 DMA"; ④ 这类"已经做对了却还有残余"的问题, 去读 HAL 源码看它到底做了什么, 往往能立刻找到被忽略的动作。

#### ★新板回板: 引脚回退 + 音量键 + 按键重映射（2026-08-24 第七轮）
- **I2S2_CK 回退 PB13→PB10**（新板按 PB10 走线）: `i2s.c`(引脚+DeInit+注释) 与 `.ioc`(`PB10.Mode/Signal=I2S2_CK`) 同步。
- **WM8960 音量控制(经 I2C1)**: `wm8960.c/.h` 新增
  - `WM8960_SetVolume(0~100)` — 线性映射到 **-60dB(0) ~ 0dB(100)**: `code = 0x3D + vol*60/100`, 写 R2/R3 时**必须带更新位 OUT1VU[8]**; 默认 80(≈-12dB)
  - `WM8960_GetVolume/VolumeUp/VolumeDown`(每档 ±5 ≈ ±3dB)
- **按键重映射(用户新板)**:
  | 键 | 功能 |
  |---|---|
  | PF0 / PF1 | 上一首 / 下一首(短按切歌, 长按快退/快进) |
  | PF2 | 暂停 / 继续 |
  | PF3 / PF4 | **预留**(将来菜单上下选择) |
  | PF5 | **菜单键(暂作"停止")** ← 原来是 PF3 |
  | PF6 | 返回键, 预留 |
  | PF7 / PF8 | **音量 + / −** |
- **实现要点**:
  - `gpio.c`: 新增 **PF6/PF7/PF8 = GPIO_MODE_INPUT + PULLUP**(普通输入, 不用 EXTI)
  - `.ioc`: 同步加 PF6/PF7/PF8(`Signal=GPIO_Input`, `GPIO_Mode=GPIO_MODE_INPUT`, `GPIO_PuPd=GPIO_PULLUP`) + `Mcu.Pin33/34/35` + `Mcu.PinsNb 33→36`
  - `Music_Driver.c` EXTI 回调: `GPIO_PIN_3 → GPIO_PIN_5` 作停止键
  - `freertos.c` 滚动定时器(50ms)里**轮询 PF7/PF8**: 按下立即调一档, 按住 >400ms 后每 200ms 连调; 并在 OLED 第 4 行显示 `音量:XX`
- **★为什么音量键用轮询而不是 EXTI**: EXTI 里做 I2C 写会**阻塞中断**且与 OLED 抢总线; 轮询(50ms)还天然支持"按住连调"。**任何需要 I2C/慢操作的按键都应放到任务里轮询或做标志位, 不要在 ISR 里直接做**。

#### ★换 PCB: DAC 改为 WM8960 —— 无声根因与初始化（2026-08-24 第六轮）
- **变更**: 新 PCB 的 DAC 从 PCM5102a 换成 **WM8960**; I2S 时钟脚从 PB10 改到 **PB13**;
  SDIN/SCLK(WM8960 控制口) 接 I2C1(PB7/PB6, 与 OLED 共总线, 地址 0x34 vs OLED 0x78)。
- **★无声的根因(两个)**:
  1. **WM8960 是 I2C 寄存器控制的 codec, 上电后 DAC/输出驱动/混音器/音量默认全是关的**
     (R25 Pwr1=0x000 VREF/VMID 关; R26 Pwr2=0x000 DAC+耳机驱动关; R47 Pwr3=0x000 混音器关;
      R34/R37=0x050 DAC→混音器通路断; R5=0x008 **DAC 默认静音**; R2/R3=0x000 **耳机音量默认模拟静音**)
     → **必须写一串初始化寄存器**, 不写就没声。(PCM5102a 是硬件配置, 不需要, 所以之前没这问题)
  2. **I2S2_CK 引脚从 PB10 改到 PB13** → `i2s.c` 原来只配 PB10, PB13 无时钟 → 数据传不出。
- **WM8960 器件地址**: 7 位 **0x1A**; STM32 HAL 传 8 位形式 **0x34**(=0x1A<<1)。
- **2-Wire 控制字(16bit)**: `CONTROL BYTE1 = (reg<<1) | ((val>>8)&1)`; `CONTROL BYTE2 = val & 0xFF`。
- **初始化序列(耳机播放, I2S 从模式, 24bit, MCLK=256fs)** — 见 `Core/Src/wm8960.c`:
  ```
  0x0F=0x000 复位
  0x04=0x000 时钟(SYSCLK=MCLK 不分频)
  0x07=0x00A 音频接口(MS=0 从模式 / WL=10 24bit / FORMAT=10 I2S)  ← 默认值正好就是这个
  0x19=0x0C0 电源1(VMIDSEL=01 2x50k + VREF)
  0x1A=0x1E0 电源2(DACL|DACR|LOUT1|ROUT1 耳机驱动)
  0x2F=0x00C 电源3(LOMIX|ROMIX 输出混音器)
  0x22=0x100 左 DAC→左输出混音器 (LD2LO)
  0x25=0x100 右 DAC→右输出混音器 (RD2RO)
  0x05=0x000 取消 DAC 数字静音 (DACMU=0)
  0x0A=0x1FF,0x0FF / 0x0B=0x1FF,0x0FF  DAC 音量 0dB + 更新位
  0x02=0x179,0x079 / 0x03=0x179,0x079  耳机音量 0dB(0x79) + 更新位
  ```
  (0dB 耳机音量码 = **0x79**; 0x7F=+6dB; 0x30=-73dB; 0x00~0x2F=模拟静音)
- **新增文件**: `Core/Src/wm8960.c`, `Core/Inc/wm8960.h`; `main.c` 在 OLED_Init 后调 `WM8960_Init()`,
  并在屏幕第1行显示 `WM8960 OK`/`WM8960 ERR`(ERR = I2C 无应答, 查接线/供电/地址)。
- **复用提示**: **换 codec 一定要确认它是"硬件配置"还是"寄存器配置"**。PCM5102a 是硬件配置(FMT 接地即可);
  WM8960/WM8994 这类是寄存器控制的, 上电默认几乎全关, **必须写初始化序列**才能出声。
  另外 WM8960 的**从机默认值(R7=0x00A: MS=0/WL=24bit/I2S)恰好匹配 STM32 主机**, 不用改。

#### 按键（EXTI PF0~PF5）
- **按键3 一按就暂停（2026-08-24，已修复）**：上电后按播放键，播放立刻又被暂停，需再按一次。
  - **根因**：**机械按键抖动**。EXTI 为下降沿触发，一次按下产生多个下降沿；第 1 个沿让 `play_state` 从 STOPPED→PLAYING（开始播放），后续抖动沿到来时状态已是 PLAYING → 被当成"暂停"。
  - **修复**：EXTI 回调里加**时间去抖**（用 `HAL_GetTick()` 记录上次动作时刻，窗口内忽略）：播放/暂停键 200ms，上一首/下一首键 50ms。原代码 `//osDelay(20);` 是无效的去抖尝试（ISR 里不能 delay）。
  - **复用提示**：**任何 EXTI 按键都必须软件去抖**（时间窗或"按下后延时确认"）。ISR 里不能用 `HAL_Delay` 去抖，只能用时间戳比较。抖动会导致状态机被多次触发。

- **★★上电"跳过第一首歌"（2026-09-22，已修复）**：
  - **现象**：一上电，第一首歌被整首跳过（直接从第二首开始）。
  - **根因（链路要看清）**：`audio_file_read` 里三个格式分支的条件都是 `if(audio_type == N && play_cmd != CMD_PREV && play_cmd != CMD_NEXT)` —— **只要开机阶段 `play_cmd` 里出现过一条 `CMD_NEXT`，三个分支全都不进，直接落到函数末尾的 `if(play_cmd == CMD_NEXT) current_index++` → 第一首被跳过**。而开机阶段 `CMD_NEXT` 的唯一来源是 `freertos.c` 滚动定时器里"PF1 短按切下一首"的判定，它的输入 `key_next_down` **只由 PF0/PF1 的 EXTI 边沿置位，而那两个中断里只判了时间、没有确认电平** → 上电瞬间 PF1 上任意一条毛刺（电源/地未稳）就被当成"短按切下一首"（50ms 后的轮询正好落在 `<300ms` 的切歌窗口里）。
  - **修复**（`HAL_GPIO_EXTI_Callback`，给**所有**按键加两道闸）：
    1. **上电宽限 `KEY_STARTUP_MS = 1000ms`**：开机 1s 内忽略一切按键边沿（上电瞬间是毛刺最密集的时刻）。
    2. **电平确认**（PF0/PF1 原来没有）：ISR 里读回 `HAL_GPIO_ReadPin(...) == GPIO_PIN_RESET` 才认这次按下 —— 真按键在 ISR 读回时必然还是低，毛刺那时已回到高，直接丢掉；**并且被丢掉时不更新去抖时间戳**，这样机械抖动的后续边沿还能重试，不会把真按键一起丢掉。
  - **复用提示**：**"电平确认 + 上电宽限"是 EXTI 按键必备的两道闸**。只认边沿的按键在任何电源扰动下都会误触发；"上电瞬间"尤其危险。另外，**遇到"跳过第一首/第一首没播"这类症状，要从"谁改了 `play_cmd` / `current_index`"倒推**——根子常常在按键中断，而不在播放逻辑本身。

#### FPU（编译）
- **`selected FPU does not support instruction`**：F407 是 Cortex-M4F 必须开硬件 FPU。CubeIDE **MCU Settings → Floating point ABI 设为 "Hardware implementation"**（-mfloat-abi=hard）。手动改 makefile 治标不治本（Clean 会覆盖）。

#### DAC / I2S
- **PCM5102a 换模块杂音**：SCK 必须接 **PC6(I2S2_MCK)**；FMT 要**接地**（I2S 标准），悬空格式不定→杂音；不同 DAC 模块引脚定义不同，换模块先核对。
- **I2S 信号完整性**：GPIO 输出速度是信号完整性参数，与播放函数无关。F407 板全 HIGH 会振铃→全噪音 → 分级（MCLK=HIGH、SCLK/SD=MEDIUM、WS=LOW）。F103 用 HIGH 正常不代表 F407 也行。
- **采样率验证**：PLLI2S 搜索 N/R 精确匹配；逻辑分析仪采样率需 ≥50MHz，WS 应=文件采样率，MCLK≈256×FS。

#### MP3 解码（minimp3）
- 解码前缓冲需 **≥1441 字节**防 bit reservoir 丢失。
- 跳过 ID3v2 标签（synchsafe 长度）。
- minimp3 一帧需 **~17.5KB 栈** → 音频任务栈 32KB，FreeRTOS 堆 48KB。
- **★MP3 直接跳过根因（2026-08-23，已定位）**：`FreeRTOSConfig.h` `configENABLE_FPU=0` → FreeRTOS 任务切换**不保存 FPU 上下文**。minimp3 用**大量浮点**(MDCT/QMF, 96处float)，音频任务被切换(读卡等待/定时器)后 FPU 被覆盖 → 解码错乱 → 首帧失败 → "MP3不支持"跳过。**FLAC/WAV 整数解码不用 FPU 所以正常**。修复：`configENABLE_FPU=1`(ARM_CM4F port lazy stacking)。
- **复用提示**：Cortex-M4F + 任务里用浮点(如 minimp3/dr_flac 浮点版)必须 `configENABLE_FPU=1`，否则浮点结果随机损坏。整数解码(自写FLAC)不受影响。
- **★MP3 定位错修复（2026-08-24）**：Queen MP3 带 516KB ID3v2 标签(内嵌封面)。FatFs `f_lseek(516762)` 大偏移**返回 OK 但定位错**(读到的不是MP3帧头0xFF)，导致 minimp3 解码异常→frame_bytes 巨大→pos 超缓冲→内存破坏(mp3_inbuf_len 变 18192>8192)→预填解29帧→跳过。根因：**SD 8MHz 4-bit 偶发读错**→FatFs 遍历FAT簇链定位错。
- **修复**：① SD ClockDiv 4→6(8→6MHz，稳定)；② ID3v2 跳过**不用 f_lseek**，改用 **f_read 循环顺序读跳过大标签**(可靠)。
- **复用提示**：大文件大偏移 f_lseek 依赖 FAT 簇链遍历，SD 读错会静默定位错。带大 ID3v2/APE 标签的 MP3 跳过标签优先用 f_read 顺序读。8MHz SDIO 在低质卡/长线上易偶发读错，默认 6MHz 更稳。
- **★minimp3 CCM 越界修复（2026-08-24）**：f_read 跳过 ID3 后仍首帧失败(d-1)，mp3_inbuf_len 变 18192>8192 → **minimp3 在 CCM 上有未定义行为/越界写**破坏相邻变量(宿主机 SRAM 正常,CCM 暴露)。修复：**mp3_dec/mp3_inbuf/mp3_pcm 全部移到 SRAM**(去 .ccmram)，mp3_inbuf 8192→4096，AUDIO_BUF_SIZE 9216→8192 腾 SRAM(余~6KB)。待实测。
- **复用提示**：minimp3(大量浮点+内联汇编)在 CCM(0x10000000)上可能有未定义行为(越界/对齐)，放普通 SRAM 更稳。CPU 密集型解码库优先 SRAM。
- **★★MP3 最终解决方案：换用 Helix 定点解码器（2026-08-24）**
  - **根因确认**：minimp3 在 STM32(ARM) 上解 **29 帧后必失败**（宿主机 x86 解 13656 帧全过）。排查排除：内存无破坏(ASan 干净, c0 b0 r0)、栈充足(s3568)、ID3 跳过正确(f520858)、文件定位正确(clean 文件 f0 也一样失败)。**结论：minimp3 此版本在 ARM Cortex-M4F 上有解码缺陷**。
  - **Helix 解码器**（`Middlewares/libhelix-mp3/`，RealNetworks 定点 MP3，专为 MCU）：**纯整数、无浮点、无 SIMD** → 避开 ARM 浮点问题。
  - **集成要点**：
    1. `assembly.h` 需改：STM32 GCC 定义 `__arm__` 而非 `ARM`，原版会掉进 `#error Unsupported platform` → 在 ARDUINO 分支条件加 `|| defined(__arm__) || defined(__ARM_ARCH)`（走通用 C，GCC 会自动用 SMULL）
    2. 补 `utils/helix_memory.h/.c`（用户下载的库缺此目录）：改为**外部内存池**（`helix_pool_init(pool,size)`），不用 malloc
    3. 内存池放进 `work_buf` **union(CMM)**：WAV重采样/FLAC解码器/Helix解码器**三者互斥复用**，不增加 CCM 占用（Helix 状态 23.8KB < FLAC 43KB）
    4. `mp3dec.h` 复制到 `Core/Inc/`（避免改 include path）
    5. `mp3_pcm` 缩到 2560 int16（原 4096），`mp3_inbuf` 4096；`mp3dec_impl.c` 清空（禁用 minimp3）
  - **验证**：宿主机 Helix 与 minimp3 输出**逐样本一致**（偏移0/均值误差0.05/最大2），集成逻辑输出与参考**逐字节相同**。
  - **复用提示**：ARM Cortex-M 上跑 MP3 优先用 **Helix（定点整数）**，不要用浮点解码器(minimp3/libmad 浮点版)；浮点解码器在 ARM 上易出解码缺陷。第三方位图库注意跨平台 `#if` 分支（`ARM` vs `__arm__`）、缺失的 utils 目录、malloc 依赖。
  - **★★★真正的直接原因（2026-08-24，关键 bug）**：`mp3_fill_buffer_region` 里判断写成
    `if (mp3_decode_next_frame() != 0) { /*解码结束*/ }` —— 但该函数**成功返回每声道样本数(1152)，失败才返回 -1**。
    **1152 != 0 成立** → 每帧解成功后立刻被当成"解码结束" → 预填只解 2~3 帧就 `file_ended=1` → "MP3预填失败"→ 跳歌。
    **根因：和 FLAC 的 `flac_decoder_decode_frame`(返回 0=成功) 语义搞混**（fill 代码是照 FLAC 模式写的）。
    **修复**：判断改为 `if (mp3_decode_next_frame() < 0)`。
    **宿主机验证**：修复后整曲 6 分钟完整播完（7681 轮, 15732736 样本, eof=1）。
  - **复用提示**：**同一模块里不同解码器的返回值约定必须统一**；照抄另一格式的调用模式时，务必核对被调函数的返回语义（0=成功 vs 样本数）。这类 bug 在宿主机上也可能因"恰好"通过而漏掉——务必用**完整播放循环**验证（不只是单帧解码）。
- **MP3 卡顿定位（2026-08-24，逻辑分析仪）：数字侧完全正常** —— WS=44107Hz 12段全程稳定、SCLK/WS=64、MCLK/WS=256、SD 数据 91886 帧无长重复段、自相关无周期性(DMA 未回绕)。**结论：I2S 数字流正确，卡顿在模拟域**（CPU 解码时电流波动耦合到 PCM5102a 输出）。旁证：同一首 FLAC 转 WAV 播放顺滑（内容相同，仅 CPU 负载不同）。
- **软件缓解（2026-08-24）**：① `assembly.h` CLZ 改 `__builtin_clz`（Cortex-M 单周期 CLZ 指令）；② 音频任务栈 32KB→8KB（Helix 栈需求极小，原 32KB 是给 minimp3 的 17.5KB 栈留的）；③ `configTOTAL_HEAP_SIZE` 48KB→24KB（**真正释放 24KB SRAM**，因堆是 .bss 固定数组，光缩栈不减 bss）；④ 工程优化 `-Os`→`-O2`。热点：DecodeHuffman 33% + PolyphaseStereo 31% + DequantBlock 15%。
- **minimp3 vs Helix 内存对比（结论：Helix 更省）**：minimp3 = 状态 6.5KB + **栈 17.5KB**（任务栈必须 ≥32KB）；Helix = 状态 23.3KB（放 CCM union 与 FLAC 复用=0 SRAM）+ **栈仅 0.5KB**（MP3Decode 112 字节）。实际 SRAM：Helix ~8KB vs minimp3 ~38.5KB。
- **★★★MP3 卡顿最终结论（2026-08-24，决定性实验）：是 CPU 负载，非软件 bug**
  - **A/B 实验**：把同一个 MP3 转成同采样率(44.1k/16bit)WAV → **WAV 顺滑、MP3 卡**（数据内容相同、同一 DMA/I2S 路径）。
  - **决定性命中实验**：在 **WAV 播放路径**里人为加 `~22ms/行` 忙等 CPU 负载（其他一切不变）→ **WAV 也变卡了**。**∴ 元凶 = CPU 负载，与 MP3 解码/驱动代码无关。**
  - **机理**：解码时 CPU 电流波动（每行 22ms 满载/24ms 空闲，~21Hz 脉冲）经电源/地耦合进 PCM5102a 模拟输出。WAV 填充几乎不占 CPU(~2%) 所以干净；FLAC 那个残留"突突"同理（脉冲频率更低）。**数字 I2S 侧已验证完全正确**（WS/SCLK/MCLK/数据/无重复）。
  - **软件只能减轻不能根治**：降 CPU（-O2、更快解码）可减轻；根治靠硬件 → **DAC 独立 LDO 供电 + 就近去耦(100nF+10uF) + 模拟/数字地分离 + 磁珠隔离**（用户计划的新 PCB）。
  - **复用提示**：**"同数据不同播放方式，一种卡一种不卡" → 优先怀疑 CPU 负载耦合，而非解码 bug。** 用"给不卡的那条路径人为加等量 CPU 负载"即可一击定性。MCU + DAC 的自制板上，CPU 电流波动干扰模拟输出是常见坑。

#### FLAC 解码（自写精简版）
- `FLAC_MAX_BLOCK=4608` 覆盖 ffmpeg/libFLAC 默认；block>4608 返回 -2。
- 支持 constant/verbatim/fixed/LPC 子帧、partitioned Rice（含 escape）、independent/left/right/mid-side。
- **解码正确性已验证（2026-08-23）**：宿主机移植解码器，ffmpeg 生成的 48k/24bit、44.1k/16bit、白/粉噪声、音乐类信号全部 **bit-exact** 匹配 → 解码算法正确，问题不在解码逻辑。
- **48k/24bit FLAC 慢+强杂音根因（2026-08-23，已定位）**：
  - 现象：WAV 16/24bit 正常；FLAC 48k/24bit（1451kbps / 1698kbps）慢 + 杂音卡顿。
  - 根因：**DMA 双缓冲刷新窗口太小**。每行 2048 半字=512 帧，@48k 约 10.7ms；FLAC 解码一个 block(4608样本≈96ms)需多次**同步读 SD**（`flac_fill` 每次只读 2048B，高比特率文件每 block 9~10 次 f_read，耗时贴近甚至超过 10.7ms）→ DMA 欠载 → 慢+卡顿+杂音。解码本身 CPU 开销很小（宿主机 30s 音乐仅 ~50ms，F407 约 9% CPU）。
  - 解决方案（两处）：
    1. `Music_Driver.h` `AUDIO_BUF_SIZE 2048→4096`（每行 1024 帧，刷新窗口 ~21.3ms@48k；`audio_buffer` 8KB→16KB，RAM 仍余 ~33KB）。
    2. `flac_decoder.h` `FLAC_IN_BUF_SIZE 2048→4096`（解码时 f_read 次数减半；CCM 预算 58.4→60.4KB，仍 <64KB）。
  - 验证：宿主机改 `FLAC_IN_BUF_SIZE=4096` 后仍 bit-exact；待用户烧录实测。
  - 复用提示：**任何 CPU 解码（FLAC/MP3）+ DMA 音频缓冲，都要保证"单次解码+读卡"最坏耗时 < 半缓冲播放时长**。缓冲行长度(半字) = 半缓冲帧数 × 4；@fs 的刷新窗口 = 半缓冲帧数/fs。若解码含同步 SD 读，缓冲要留足余量。
  - **实测诊断（2026-08-23 第二轮）**：加 OLED 诊断"解码:XXXms SD:X"，用户报 **解码=27ms（单 block 含读SD最慢），SD=4(4-bit)** → 27ms > 21.3ms 窗口，确认欠载。解码 CPU 只占 ~3-4%，大头是解码中同步 f_read 读卡（~23ms/block）。
  - **修复2（2026-08-23 第三轮）**：`AUDIO_BUF_SIZE 4096→8192`（每行 2048 帧，窗口 ~42.7ms，覆盖 27ms+余量；audio_buffer 16KB→32KB，RAM 余 ~19KB；CCM 不变——wav_raw 变大但 union 仍由 flac_decoder 主导；wav_raw=32768 保证 96k/32bit WAV 不截断）。待实测。
  - **若 8192 仍不够**：做"解码先行"(预解码)把解码从 DMA 刷新时点解耦，或减少 f_read 次数(把 MP3 缓冲挪出 CCM 给 FLAC_IN_BUF_SIZE 增到 8192)。
  - **当前状态（2026-08-23 第四轮，缓冲加大无效，转向 SD 时钟）**：
    - 已改：`AUDIO_BUF_SIZE 8192→10240`（窗口 42.7→53.3ms）+ `FLAC_IN_BUF_SIZE 4096→6144`（读卡 5→3 次/block）→ **仍无改善**。
    - 诊断结论：解码 **27ms/block**（含读卡，CPU 仅 ~2ms，其余全是同步读 SD）；PLLI2S **N344/R7 OK**（48k 时钟正确）；SD **4-bit**；WAV 正常。
    - **推论**：加大缓冲窗口到 53ms 仍不够 → 单行缓冲最坏仍可能需 2~3 个 block 解码(27ms×3=81ms>53ms)，且读卡**固定开销主导**（每 block 25ms 读卡，数据量只 ~17KB）。
    - **下一步（用户决定）**：提高 SD 时钟 `ClockDiv 8→6`（4.8→6MHz，4-bit 3MB/s）。8MHz 之前偶发失败，6MHz 是折中。若仍不够 → 解码先行。
    - **诊断测量 bug 已修**：速率计数器播放开始时重置（之前 57/s/73/s 是开机累计垃圾值）。
    - **逻辑分析仪结论（2026-08-23 第五轮，决定性）**：WS = **48000Hz 精确**，SCLK = 64×WS 正确 → **I2S 数字侧完全正常，真实采样率就是 48kHz**。"慢放+音调变低"不是时钟问题，是 **DMA 欠载→数据重复**。
    - **诊断实测定性（B4608 / 055 / 026）**：读卡 **55ms/s**（每block~5.3ms，非瓶颈）；解码 **26ms/block**，减去读卡后 **~21ms 是 CPU 逐位解析**（M4F 上高比特率 FLAC 每 block ~14 万 bit 很慢）；block=4608 大块 → 一行 2560 帧**最多解 1 个 block=26ms < 53ms 窗口** → **理论上不该欠载**，但用户实测慢+突突。
    - **下一步（待用户上传文件）**：宿主机逐样本对比用户真实 FLAC 文件。bit-exact → 解码正确，优化解码器降 CPU（flac_bits 逐位改块读）；不 exact → 解码器对真实文件有 bug（可能旧 ffmpeg 编码特性），直接修。
    - **SD 时钟回退 6MHz**：读卡非瓶颈（55ms/s），8MHz 无收益且有偶发失败风险。
    - **用户真实文件宿主机验证（2026-08-23 第六轮，决定性）**：用户上传"万能青年旅店-早.flac"（48k/24bit/83.65s/872帧/1469kbps）。`flac_test` 逐样本对比 ffmpeg → **4015242 帧 0 差异，完全 bit-exact**。且**重采样进 DAC 的数据**（play_sim2 模拟 flac_fill_buffer_region）相邻样本跳变统计与参考**几乎完全一致**（81.19% vs 81.21%）→ **解码器 + 重采样数据全部正确**。
    - **宿主机 CPU 估算**：F407 解码整个文件约 3.24s CPU（3.9%），但 M4F 实际 flac_bits 逐位可能 5-10× 慢 → **CPU 解码可能是 26ms 的大头**（读卡仅 55ms/s）。理论一行最多解 1 block=26ms < 53ms 窗口，但 CPU 慢 + 读卡突发可能偶发超。
    - **下一个诊断（第六轮）**：加 `flac_fill_ms_max`（单次 fill 最慢耗时，含解码+重采样+读卡），OLED 显示 `F:XXX D:YYY R:ZZZ`。F > 53ms → 欠载确认；F < 53ms → 欠载非 fill 时间，问题在硬件/调度。
    - **fill 实测 F028 < 53ms（第六轮结果）**：fill 最慢 28ms 不欠载，但 WAV 顺滑/FLAC 突突 → 问题在 FLAC 路径。**用户关键线索：block 1026 的 FLAC 顺滑，block 4608 突突。**
    - **block 大小根因（第七轮）**：block 4608 时约 11% 的行对出现"连续两行跨 block 边界"，每行解 1 block(26ms)，连续 2 行=52ms 逼近 53ms 窗口，叠加读卡/调度偶发即超时欠载→突突。block 1026 解码均匀无尖峰→顺滑。
    - **解码器优化（第七轮）**：`flac_bits` 逐字节→**32 位位缓冲**(flac_refill)；`flac_rice` unary 直接位缓冲数零。宿主机验证用户真实文件+3测试文件**全 bit-exact**。目标 F407 解码 26→~18ms。待实测。
    - **若仍突突**：行缓冲大小对齐到 4608 因数（消除连续跨块行）。
    - **优化实测（第八轮）**：F 28→23ms，D 26→21ms，R 64→42ms/s → 播放"顺滑很多能听了"，但**偶尔还卡**（连续跨块行 46ms 逼近 53ms + 读卡突发偶发超）。
    - **修复（第八轮，根除尖峰）**：`AUDIO_BUF_SIZE 10240→9216`（行 2304 帧 = **4608/2**，每个 block 正好供给 2 行）→ 消除"连续跨块行"的 2×解码尖峰叠加。窗口 48ms，fill 21ms，余量 27ms。待实测。
    - **block 大小权衡（用户问）**：block ≤ 行大小(2304帧)时 fill 均匀无尖峰(block1026顺滑即证)；但继续减小收益递减(固定开销占比升、压缩率降)。**最优 block≈4096**。不靠转码小 block，播放器对齐即可。
    - **★真正根因（第九轮，逻辑分析仪 TEST-FLAC2）：WS = 44106Hz 不是 48000！** 48k 的 PLLI2S 配置(344/7)**根本没生效**，硬件残留 44.1k 配置(271/6→44106Hz)播放 48k 文件 → **慢 8% + 音调低 8%**，完全吻合用户症状。之前所有解码/缓冲/SD 优化都是次要的。
    - **根因机制**：`HAL_RCCEx_PeriphCLKConfig` 改 PLLI2S N/R 时 PLLI2S 在运行，**没先禁用** → 若没重新锁定到新频率，**寄存器显示新值(344/7, RDY=1)但实际输出旧频率(271/6)**。用户 WAV 是 44.1k(271/6 首配成功)，48k FLAC 切换失败残留 44.1k。
    - **修复**：`i2s_set_sample_rate` 配置前 `__HAL_RCC_PLLI2S_DISABLE()` + 等 RDY 清零，再 HAL 配置(使能+重新锁定)。待实测 WS 是否变 48k。
    - **复用提示**：STM32F4 PLLI2S 改频率必须**先禁用再使能**，否则残留旧锁；RDY=1 不代表锁到新频率（旧锁也 RDY=1）。诊断要**实测 WS 频率**（逻辑分析仪），不能只看寄存器。
    - **修复验证（第九轮 TEST-FLAC3）**：WS=47991Hz（48k 正确）→ PLLI2S 修复生效，慢放/音调低解决。但**仍有周期突突**。SD 数据解码分析：**无重复段(0%)、无孤立尖峰(0)** → **数据完全正确、无 DMA 欠载**。
    - **突突真根因（第十轮）**：同一首 48k WAV 顺滑、FLAC 突突，但数据都正确无欠载 → **突突 = FLAC fill 的 21ms 解码 CPU 尖峰**（每 2 行一次，block 4608 时）对 DAC/时序的扰动。WAV fill 仅 2ms 无尖峰所以顺滑。
    - **修复（第十轮，用户要求两项）**：① TF 卡 ClockDiv 6→4(8MHz)；② **解码先行(预解码)**：播放循环空闲时，若当前 block 已用完则预解码下一个 block，fill 只做快速重采样（F 应从 21ms→~2ms），消除解码尖峰。仅对 block 与行对齐(4608=2*2304)完美预解码，其它 block 由 fill 兜底。待实测。
    - **预解码失败（第十一轮，模拟证实）**：宿主机模拟播放循环发现 fill 内部解码 873 次、预解码仅 1 次——**fill 每 2 行在 block 末尾先内部解码，预解码永远追不上**。单 pcm 缓冲无法安全预解码(提前会覆盖未用数据)，**放弃预解码**（代码已移除）。
    - **-O2 建议（第十一轮）**：宿主机 -Os(0.21s) 比 -O2(0.19s) 慢 ~10%，CubeIDE 默认 -Os 拖慢解码。建议工程优化级别 -Os→-O2。flac_refill 补24位优化有 bug 已回退(bit-exact 恢复)。
    - **剩余突突**：fill 每 2 行 21ms 解码 CPU 尖峰（<48ms 窗口不欠载，但可能扰动 DAC）。软件优化到头；若 -O2 后仍突突 → 硬件电源/模拟端，或 foobar 转 block 2048。

#### ★多语言文字库搬到 W25Q64（2026-09-22）
- **需求**: 简中/繁中/日/英/韩/俄 六种文字; 字库存 W25Q64; 按需读取显示到 OLED。
- **★关键设计(别按语言拆字库)**: 简/繁/日汉字在 Unicode 里**同在 U+4E00–U+9FFF**(20992 码位), 拆成三份会重复三遍。
  正解 = **一个文件 + 内部按"连续 Unicode 区段"组织**, 查字是纯算术 `偏移 = data_off + (码点 - start_cp) * bpg`
  → **没有索引表, 不额外占 RAM**。
- **★ASCII 不放进字库**: 实测预览发现, 16×16 格子里用 TTF 渲染 8 像素宽的 'M'/'W' 会被切掉半个字;
  而内 flash 已有的 `OLED_F8x16`(8×16, 1.5KB) 本来就是为此设计的点阵字体, 显示更好且永远可用 → 留作第 1 级取字 + 最终兜底。
- **取字三级回退**: 内 flash F8x16(ASCII) → W25Q64 字库(俄/日/韩/汉字/全角) → 内 flash GB2312 表(兜底,
  改 `OLED_USE_INTERNAL_GB2312=0` 可省掉那 230KB) → 画 `?`。
- **体积(16×16, 1bpp)**: 六种文字全含 **≈1.01MB**(汉字 656KB + 谚文 349KB + 其余约 25KB)。W25Q64 的 8MB 此前完全没用过, 够放。
- **实现**: `tools/gen_font.py`(TTF→bin: CRC32 + 区段表 + `--selftest` 几何图案自检 + `--preview` 终端预览 + `--px/--y-off` 调参);
  `Core/Src/font_format.c`(**纯逻辑**: 解析/查表/96 槽 LRU 缓存); `Core/Src/font_store.c`(W25Q64 读 + SD 烧写/回读校验);
  `OLED.c` 的字符串显示与滚动缓冲统一接到新引擎(UTF-8 解码扩到 1~3 字节, 4 字节 emoji 跳过)。
- **烧写方式**: SD 根目录放 `font16.bin` → 开机自动: 校验源文件 CRC → 逐 4KB 扇区擦写 → 回读整段算 CRC 比对 → 显示进度/结果;
  与 Flash 里内容相同则跳过(不会每次开机重复写)。**全程不用 `f_lseek`**(沿用之前"大偏移定位会静默出错"的教训), 需要回头读就重新 `f_open` 顺序读。
- **★验证方式(值得复用)**: 把"格式解析/查表/缓存"写成**不含任何 STM32 头文件的纯逻辑**, 用假 Flash 数组 + `font_read_fn` 回调,
  **在 PC 上用 gcc 跑单元测试**(`tools/test_font_format.c`): 头部解析 / 区段越界保护 / CRC 自洽 / 混合宽度(8×16 与 16×16)查字字节一致 /
  缺字回退 / LRU 淘汰后仍正确 / **重复查同一字 Flash 访问增量=0**。→ **能在 PC 上跑的逻辑就搬到 PC 上验**, 比烧板子试快得多。
- **性能结论**: 读一个字形 32 字节(SPI1@42MHz)约 6µs, 而 OLED 整屏刷新走 I2C 要十几毫秒 → **字库读取不是瓶颈**; 加 LRU 缓存后滚动字幕完全不碰 SPI。
- **★启动速度优化(2026-09-23)**: 原方案每次开机都检查 SD 上的 font16.bin(还要比较 CRC), 实测明显拖慢启动 →
  改成 **只在"开机时按住 PF7(音量+)"时才走 SD 检查/烧写**; 常规启动只 `font_store_init()` 从 W25Q64 读已烧好的字库,
  完全不碰 SD。字库没烧过也不影响使用(中文走内 flash 兜底)。
- **待实测**: 真字体生成的字库在屏上的观感(建议先用 `--preview` 调好字号/偏移再烧)、烧写耗时、长时间滚动是否流畅。

#### ★FreeRTOS 任务栈（2026-09-22 踩雷）
- **现象**: 新加的字库流程，开机显示完"字库检查中..."就**黑屏**。
- **根因**: `defaultTask` 的 `stack_size = 128*4 = **只有 512 字节**`(CubeMX 默认给的小值)，而"字库检查/烧写"被挂在这个任务里 ——
  它要 `sprintf`(newlib 自身就吃约 250B 栈) + FatFs `f_open/f_read` + 新的 OLED 取字引擎(多层嵌套调用) → **栈溢出**，
  静默踩坏相邻内存(其他任务的 TCB/栈、FreeRTOS 堆里的对象) → 行为不可预期(先画出一句话、随后黑屏)。
- **★放大伤害的关键**: `configCHECK_FOR_STACK_OVERFLOW` **默认没开** → 溢出**没有任何提示**，只表现为"随机崩/黑屏"，极难定位。
- **修复**: ① 字库流程挪到 `FILE_LOAD`(3KB 栈)；② `defaultTask` 栈 512B→1KB；
  ③ **打开 `configCHECK_FOR_STACK_OVERFLOW = 2` 并实现 `vApplicationStackOverflowHook`** —— 一旦某任务栈被写穿，
  直接把**出问题的任务名显示到 OLED 上**，不再黑屏；④ `AUDIO_READ` 改成"等扫描完成后再 `OLED_Clear()`"——
  它原来在任务开头就清屏，会把开机的字库/扫描提示擦掉，也是"黑屏"假象的来源之一。
- **复用提示**: ① **CubeMX 生成的 `defaultTask` 默认栈很小(512B)**，任何往里塞 FatFs/sprintf/复杂库调用的改动都可能溢出 →
  要么加大栈、要么把活挪到栈大的任务去做；② 栈需求可用 `Debug/**/*.su` 逐层相加估算(注意 newlib 的 `sprintf/vfprintf` 很吃栈)；
  ③ **`configCHECK_FOR_STACK_OVERFLOW` 必须开**，且钩子要"能看见"(本工程显示到 OLED) —— 否则内存类故障只能靠猜。

#### ★多语言文件名打不开（俄语/日语歌报"文件损坏/跳过"）（2026-09-23）
- **现象**: 俄语、日语的音乐文件每次播放都显示"文件损坏/跳过"；中文文件一直正常。
- **根因（链条）**: `FATFS/Target/ffconf.h` 是 `_CODE_PAGE=936` + `_LFN_UNICODE=0` → FatFs 列目录/开文件都要用 `ff_convert()`
  在 OEM(GBK) 与磁盘上的 UTF-16 长文件名之间转换。而本工程原来的 `ff_unicode.c` 是**拿 GB2312 区位码表实现的**，
  那张表**只填了 16~87 区的汉字，1~9 区（全角字符/平假名/片假名/希腊/西里尔）全是 0** → 俄文（西里尔在 GB2312 第 7 区）、
  日文假名（第 4/5 区）**转换失败** → FatFs 用 `?` 代替 → `f_open` 拿这个名字找不到文件 → WAV 路径报"文件损坏/跳过"
  （该分支把"打开失败"和"文件真损坏"合并成一个提示，掩盖了根因）。中文歌名一直能放，正因为汉字在表里。
- **修复**: ① `ff_unicode.c` 换成**完整 cp936 表**（`tools/gen_ff_unicode.py` 用 Python 的 cp936 编码器生成）：
  GBK→Unicode 21791 项 + Unicode→GBK 汉字 20902 项 + 其它 889 项，约 **91KB** 内 flash；
  ② `OLED_GBKString_To_UTF8()` 改用**同一套** `ff_convert()` —— 否则文件能打开但歌名会显示成 `?`；
  ③ `OLED_ShowGBKString()`（死代码）改为"先转 UTF-8 再走新引擎" → 顺带让 16KB 的旧区位表退役；
  ④ 扫描文件名改成**有界 `strncpy`**（原来是无界 `strcpy`，长名字有溢出风险）；
  ⑤ 长名里出现 `?`（cp936 转不了的字符，如韩文音节/emoji）时**退回 8.3 短名**，保证文件仍能打开（显示成 `KOREAN~1.MP3`，但能播）。
- **★验证方式（可复用）**: 生成转换表时**同时**用 Python 的 cp936 导出全部期望值，宿主机用 gcc 编译 `ff_unicode.c` +
  `tools/test_ff_unicode.c`，**逐条比对全部 21791 个映射**（不是抽样），并模拟"UTF-16 文件名 → GBK → 转回 UTF-16"
  与磁盘上的名字逐字相同 → 证明 `f_open` 能匹配上。**能在 PC 上验的就别烧板子试。**
- **已知限制**: cp936 里**没有韩文音节**（한글 转不了）→ 韩文长文件名会退回 8.3 短名显示（能播，名字不好看）；emoji 同理。
  彻底解决需要改 `_LFN_UNICODE=1`（文件名全程 UTF-16），那样整个工程的字符串处理都要跟着改，属较大重构。
- **复用提示**: ① **FatFs 的 OEM 转换表必须覆盖"实际会出现的字符"** —— 只做 GB2312 的表看似"支持中文"，
  实际会把俄/日/韩/繁体文件名全部挡在门外，而且症状是"文件损坏"这种误导性提示；② **遇到"某些文件打不开、另一些正常"，
  先怀疑文件名编码，而不是文件内容**；③ 把"打开失败"和"内容损坏"合并成一个提示会掩盖根因，应分开显示。

#### ★★MP3"能播但声音不对"的真根因：封面图里的伪同步字（2026-09-23）
- **现象**: MP3 能播但声音不对（WAV/FLAC 同板同卡都正常）。
- **宿主机复现（把工程里的 `mp3_decode_next_frame()` 原样抽出来在 PC 上跑真文件）**：用 `Queen - Bohemian Rhapsody.mp3`
  （ID3v2.3 标签 **516,762 字节 = 504.7KB**，里面是 PNG/JPEG 封面；音频本身 44.1k/立体声/320kbps CBR）：
  - 从**标签内**开始（= 模拟"跳过标签失败"）→ 解码器报 **576 样本/声道、1 声道、16000 Hz**，1.3 万帧全是垃圾 ✗
  - 从**真正音频起点**（偏移 516762）开始 → 1152 样本/声道、2 声道、44100 Hz ✓ 正常
  - **根因**：封面图里的 **JPEG 标记字节 `FF E0~FF EF` 与 MP3 同步字 `0xFFEx` 完全同形** → 解码器锁到伪同步字上。
    更糟的是 `mp3_hz`/`mp3_channels` 是**从解出的第一帧取的** → 被记成 16kHz/单声道 → 重采样步进和声道解析全错 →"声音不对"。
- **为什么跳过标签会失败**：定位第一帧只靠"找同步字"太脆弱——ID3 标签跳过那一步**只要有闪失**（读卡偶发错误让跳过循环
  提前 `break`），文件位置就停在标签中间，随后必然误命中封面里的伪同步字。
  **WAV/FLAC 不依赖"找同步字"**（起始位置来自解析文件头的 `data` 块偏移），所以它们一直正常。
- **修复（根治）**：
  1. 新增 `mp3_hdr_info()` / `mp3_find_valid_frame()`：候选同步字处必须能解析出**合法帧头**，并且**按帧长跳到下一帧也是
     合法帧头、且采样率/声道一致** —— 伪造的同步字几乎不可能同时满足，直接一票否决；
  2. `mp3_decode_next_frame()` 里用这个"**连续两帧头校验**"取代单纯 `MP3FindSyncWord`；
  3. 顺便**用帧头里的采样率/声道直接确定** `mp3_hz`/`mp3_channels`（比从解码结果取更早、更可靠）；
  4. ID3v2 标签跳过循环**读失败重试 5 次**再放弃（原来直接 `break` → 位置留在标签中间）。
- **★验证（宿主机，决定性）**：修复后从**标签内**开始解码，与"手动切掉标签"的参考结果 **逐样本完全一致**
  （13655 帧 / 15,730,560 样本，最大差异 **0**）；无标签的合成文件回归正常（L/R 完全一致、FFT 主峰 440.1Hz、帧边界无跳变）。
- **复用提示**：① **MP3 定位第一帧不能只认同步字** —— 内嵌封面（JPEG）里全是 `FFE0~FFEF`，必须用"连续两个帧头"校验；
  ② **解码器参数（采样率/声道）要从帧头取，不要从"第一帧解码结果"取** —— 一旦第一帧是伪同步字，参数就全错且一路错到底；
  ③ **"能播但声音不对"要查起始定位，不要只怀疑解码算法**；④ 这类问题在宿主机上可以 100% 复现（把解码逻辑原样抽出来跑真文件）。

### 工程文件
- `.ioc` 文件名带**前导空格**会让 CubeMX 打不开 → 重命名去空格。
- 改完链接脚本/CCM 后必须 **Clean + Build**，否则烧的是旧固件（看 `Debug/` 下 `.elf` 时间确认）。
- 文件混用 tab/空格缩进时 Edit 易失败 → 用 python 按行精确替换。

---

## 二、F103 音乐播放器（旧工程）

**位置**：`D:\Users\15754\Desktop\Music_Player_FreeRTOS`

- 与 F407 的关系：F407 是从本工程移植的；OLED.c 注释里 F103 从 i2c.h 引入句柄，F407 改为 extern 引用 main.c 的 hi2c1。
- WAV 播放框架沿用；FLAC 解码器沿用（F407 上可调大 FLAC_MAX_BLOCK）。

---

## 三、桌面工具项目（OCR / 计算器 / 串口调试）

> 待补充：有实际排错记录后，按模板填入并回填到技能速查表。

---

## 附：其他项目速查

- **DengFOC STM32 移植**：main 纯源码 / PCB 文件走分支（见 GitHub Idontwanastudy/DENG-FOC-STM32-PORTING）。
- **OCR 工具**：PyQt5 + PaddleOCR 桌面工具（悬浮球 + 主窗口，拖放/粘贴/打开自动识别，可存 TXT）。
